# main 브랜치 리뷰 — 2026.04.17

README/handoff가 주장하는 완료 범위와 실제 구현 품질 사이 간극이 큽니다. 다음 LLM이 바로 집어갈 수 있도록 근거와 개선 포인트를 정리합니다.

---

## 1. "다중 세트 / 3카테고리" — 사실상 find/replace 복붙

- `lib/features/{hangul,alphabet,numbers}/presentation/*_learn_screen.dart` 세 개 모두 **정확히 368 LOC, diff는 타입/문자열 치환 9줄 수준**
  - 예: `HangulLearnScreen` → `AlphabetLearnScreen`, `'basic_consonants_1'` → `'alphabet_letters_1'`
- 퀴즈 스크린도 동일 패턴
  - `hangul_quiz_screen.dart` 777 LOC
  - `alphabet_quiz_screen.dart` 777 LOC
  - `numbers_quiz_screen.dart` 826 LOC
  - 서로 find/replace 복붙
- Repository 4개도 73 LOC 동일 구조
- **공통 추상화(BaseLessonScreen / generic Quiz 엔진) 0개.** 버그 하나 생기면 3곳 동시 수정 필요

## 2. "garage tone UI polish" — 디자인 시스템 없음

- `lib/app/theme/kid_theme.dart` (144 LOC) + `ToyButton` (178 LOC) + `ToyPanel` (74 LOC) 뿐, 나머지는 전부 인라인 Container+Color
- spacing/duration 토큰 없고 매직넘버가 모든 화면에 흩뿌려짐
  - `compact ? 12 : 18`
  - opacity `0.94 / 0.88 / 0.76 / 0.58`
  - `hangul_quiz_screen.dart:150` — `final sectionGap = isTight ? 10.0 : (isCompact ? 14.0 : 20.0)`
- 라우팅은 전부 `Navigator.push(MaterialPageRoute(...))`
  - `hero_screen.dart:27`, `home_screen.dart:192`
  - go_router / named routes 없음

## 3. "자산" — 실제 파일 1개

`find assets -type f ! -name '.keep'` 결과:

- 이미지 실제 파일: **`hero_face.png` 단 1장**
- 오디오 파일: **0개** (voice/sfx/music 폴더 전부 `.keep`)
- 카테고리/스티커/배경 이미지: 없음
- 학습 카드는 JSON의 bare text (`"symbol": "ㄱ"`) 를 Container에 렌더링. 일러스트 0

## 4. "음성 cue" — flutter_tts 기본 호출

- `pubspec.yaml` 의존성: `shared_preferences`, `flutter_tts`, `cupertino_icons` 뿐
- `speech_cue_service.dart`가 그냥 `flutter_tts.speak(text, locale: 'ko-KR')`
- 녹음본 / 성우 / 캐릭터 내레이션 없음. 27개월 대상인데 TTS만

## 5. "스티커 / 리워드" — 숫자 카운터

- `progress_store.dart`의 `stickerCount`는 그냥 int. 수집 UI / 애니메이션 / 보상 장면 전무
- 정답 피드백은 `AnimatedOpacity + AnimatedScale` 180ms + `'딩동댕!'` 텍스트 (`answer_feedback_overlay.dart:50`)
- 폭죽 / 사운드 / 캐릭터 리액션 없음

## 6. "보호자 메뉴 숨김 진입" — 단순 카운터

- `hero_screen.dart:18–30`: 얼굴 5회 탭하면 `Navigator.push`. 쿨다운 / 힌트 / 시각 피드백 없음
- 반면 `avatar_setup_screen.dart`는 **1152 LOC 단일 파일** — 토글 / 버튼 / 진도 표시를 한 화면에 몰아넣은 대시보드
- 아이 UX에 붙인 가장 큰 화면이 보호자 메뉴라는 LOC 비율 자체가 이상

## 7. "회귀 테스트" — 스모크 수준

- `test/` 20개 파일이지만 대부분 "hero 보인다 / home 보인다 / 탭된다" 위젯 스모크
- 퀴즈 채점 로직 / 진도 계산 / 오답 리플레이 유닛 테스트 거의 없음

---

## 다음 LLM에게 넘길 키포인트

1. **Quiz/Learn 3종 복붙을 공통 제네릭 엔진으로 뽑기** — 가장 큰 허접의 근원
2. **실제 일러스트 / 사운드 자산 파이프라인 채우기** — 현재 이미지 1장, 오디오 0개
3. **디자인 토큰화** (spacing / radius / duration / opacity) 후 매직넘버 제거
4. **스티커·리워드 실제 시각화** (컬렉션 / 애니메이션 / 효과음)
5. **라우팅 정리** (go_router 또는 최소 named routes) + `avatar_setup_screen.dart` 1152 LOC 분해
6. **TTS를 넘어선 실제 음성 / SFX** 도입

문서는 "다 됐다"고 말하지만 코드상으로는 "스캐폴딩 끝, polish 시작 전" 상태입니다.
